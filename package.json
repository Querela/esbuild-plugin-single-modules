{
  "name": "esbuild-plugin-single-modules",
  "description": "esbuild plugin to split bundles into modules for each import",
  "version": "1.0.0",
  "license": "MIT",
  "keywords": [
    "esbuild",
    "plugin",
    "modules"
  ],
  "author": "Querela <ekdev@live.de>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Querela/esbuild-plugin-single-modules.git"
  },
  "bugs": {
    "url": "https://github.com/Querela/esbuild-plugin-single-modules/issues"
  },
  "homepage": "https://github.com/Querela/esbuild-plugin-single-modules",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^22.12.0",
    "typescript": "^5.7.3"
  },
  "peerDependencies": {
    "esbuild": ">=0.23.0"
  }
}
